# ══════════════════════════════════════════════════════
# Agentic FusionDTI — 의존성 목록
# Python 3.10+ 권장
# ══════════════════════════════════════════════════════

# ── 딥러닝 코어 ───────────────────────────────────────
# torchvision/torchaudio 설치 금지: transformers와 버전 충돌 발생
# torch 2.6+ 필수 (CVE-2025-32434 대응)
torch>=2.6.0
transformers>=4.36.0
accelerate>=0.24.0

# ── 화학정보학 (약물 인코딩) ───────────────────────────
rdkit>=2023.3.1

# ── 데이터 처리 ───────────────────────────────────────
pandas>=2.0.0
numpy>=1.24.0
scipy>=1.10.0
tqdm>=4.65.0

# ── Hugging Face Hub (모델 다운로드) ──────────────────
huggingface_hub>=0.20.0

# ── 경량화 (4-bit / 8-bit 양자화 + LoRA) ─────────────
bitsandbytes>=0.41.0
peft>=0.9.0

# ── 에이전트 프레임워크 ────────────────────────────────
smolagents>=0.2.0

# ── 백엔드 / 프론트엔드 ───────────────────────────────
fastapi>=0.104.0
uvicorn>=0.24.0
streamlit>=1.28.0

# ── 통계 시각화 ───────────────────────────────────────
matplotlib>=3.7.0
seaborn>=0.12.0
