# Bio-AI DTI Query Pipeline — Python 의존성
# Python 3.10+ 권장
# 설치: pip install -r requirements.txt
# 주의: torchvision/torchaudio 설치 금지 (transformers와 버전 충돌)

# ── 딥러닝 코어 ───────────────────────────────────────────────
# torch 2.6+ 필수 (CVE-2025-32434 대응)
torch>=2.6.0
transformers>=4.36.0
accelerate>=0.24.0

# ── 모델 다운로드 ─────────────────────────────────────────────
huggingface_hub>=0.20.0

# ── 화학정보학 (약물 SMILES 처리) ────────────────────────────
rdkit>=2023.3.1

# ── 데이터 처리 ───────────────────────────────────────────────
pandas>=2.0.0
numpy>=1.24.0
scipy>=1.10.0
tqdm>=4.65.0

# ── 대시보드 ──────────────────────────────────────────────────
streamlit>=1.28.0

# ── 시각화 (scripts/plot_poster_figures.py) ───────────────────
matplotlib>=3.7.0
seaborn>=0.12.0

# ── 학습 전용 (train.py 실행 시 필요) ────────────────────────
# 4-bit 양자화 (--quant 4bit 옵션 사용 시)
bitsandbytes>=0.41.0
# LoRA fine-tuning (--lora 옵션 사용 시)
peft>=0.9.0

# ── 데이터 로딩 전용 (prepare_sequences.py 실행 시 필요) ──────
# pip install DeepPurpose  (별도 설치, PyPI 버전)
